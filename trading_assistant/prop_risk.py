# prop_risk.py — coming next: daily loss tracking, trailing drawdown monitor, lockout state
# This file will extend SessionState with a stateful risk tracker that updates
# after each trade and enforces prop firm rules across the session.
